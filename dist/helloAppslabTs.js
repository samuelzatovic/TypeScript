function totalDistance(height, length, steps) {
    let totalDistance = (steps / height) * (length + height);
    return totalDistance;
}
console.log(totalDistance(0.2, 0.4, 100));
//# sourceMappingURL=helloAppslabTs.js.map